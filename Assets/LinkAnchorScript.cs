using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LinkAnchorScript : MonoBehaviour
{
    [Tooltip("Can other anchors find and attach to this?")]
    public bool isRecipient = true;

    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
